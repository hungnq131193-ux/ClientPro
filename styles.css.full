/* --- Thay dòng @import cũ bằng dòng này --- */
@import url("https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap");

:root {
  --app-height: 100dvh;
  /* iOS Safe Area (PWA/standalone). Defaults to 0 on Android/Desktop. */
  --sat: 0px;
  /* safe-area-inset-top */
  --sab: 0px;
  /* safe-area-inset-bottom */
  --sal: 0px;
  /* safe-area-inset-left */
  --sar: 0px;
  /* safe-area-inset-right */
}

/* Copy created from assets/styles.css via ChatGPT GitHub connector. Note: source fetch was truncated by connector, so this backup may be incomplete. */
